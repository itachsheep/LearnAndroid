<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <data>
        <variable
            name="viewModel"
            type="com.tao.databind_lamda.viewmodel.LambdaViewModel"/>

        <variable
            name="employee"
            type="com.tao.databind_lamda.model.Employee"/>

    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
       >

        <Button
            android:id="@+id/lambda_bt1"
            android:layout_marginTop="10dp"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:textSize="25sp"
            android:text="@{employee.firstName}"
            android:onClick="@{viewModel.test}"
            />

        <Button
            android:layout_marginTop="10dp"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/lambda_bt1"
            android:textSize="25sp"
            android:onClick="@{() -> viewModel.onEmployeeClick(employee)}"
            />

    </android.support.constraint.ConstraintLayout>
</layout>
